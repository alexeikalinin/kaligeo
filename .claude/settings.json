{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"/Users/alexei.kalinin/Documents/VibeCoding/AI Audit\" && git add -A && git diff --cached --quiet || git commit -m \"auto: $(git diff --cached --name-only | tr '\\n' ' ' | sed 's/ $//')\" --no-gpg-sign 2>&1 | tail -1"
          }
        ]
      }
    ]
  }
}
